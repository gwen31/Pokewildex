// import { useState } from "react";

function SearchBar() {
  return (
    // const { filteredPokemon, setFilteredPokemon } = useState([]);
    <>
      <input type="text" placeholder="Please enter a pokemon name..." />
    </>
  );
}
export default SearchBar;
